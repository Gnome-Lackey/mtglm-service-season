name: ${self:provider.stage}-mtglm-season-metadata-get
handler: build/handlers/getSeasonMetadata.handler
tags:
  get: player
events:
  - http:
      method: get
      path: seasons/{seasonId}/metadata
      authorizer: ${file(partials/common/authorizer.yml)}
      cors: ${file(partials/common/cors.yml)}
      documentation:
        summary: "GET season metadata"
        description: "get season metadata"
        requestHeaders:
          - name: "Authorization"
            description: "Authorization token is needed to access the endpoint."
        methodResponses:
          - statusCode: "200"
            requestModels:
              "application/json": SeasonMetadataResponse
          - statusCode: "500"
iamRoleStatements:
  - Effect: Allow
    Action:
      - dynamodb:Query
    Resource: 
      - ${self:custom.SeasonMetadataTableArn}

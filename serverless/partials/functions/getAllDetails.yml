name: ${self:provider.stage}-mtglm-seasons-get-all-details
handler: build/handlers/getAllDetails.handler
tags:
  get: season
events:
  - http:
      method: get
      path: seasons/details
      authorizer: ${file(partials/common/authorizer.yml)}
      cors: ${file(partials/common/cors.yml)}
      documentation:
        summary: "GET all Season details"
        description: "Get all Season details"
        requestHeaders:
          - name: "Authorization"
            description: "Authorization token is needed to access the endpoint."
        methodResponses:
          - statusCode: "200"
            requestModels:
              "application/json": SeasonsResponse
          - statusCode: "500"
iamRoleStatements:
  - Effect: Allow
    Action:
      - dynamodb:GetItem
      - dynamodb:Scan
    Resource:
      - Fn::GetAtt: [SeasonsTable, Arn]
      - Fn::ImportValue: ${self:provider.stage}-mtglm-PlayerTableArn
